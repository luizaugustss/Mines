export function play(e) {
  const __ = e.target;
  
  if (__.id.startsWith("col")) {
    
    number_mine(wid, len, matriz, width_board, length_board)

  }else{
        console.log("click:"+ __.id);
  }
 
}
